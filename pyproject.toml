[project]
name = "cloud-server-env"
version = "0.1.0"
description = "Cloud Server RL Environment"
requires-python = ">=3.8"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv>=0.2.0"
]

[project.scripts]
server = "app:app"
